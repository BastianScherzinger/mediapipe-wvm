# ── Zugangsdaten — NIEMALS ins Repository ───────────────────────────────────
.env
.env.local
.env.*.local
*.key
*_auth.json
data/higgsfield_mcp_auth.json

# ── Laufzeitdaten ───────────────────────────────────────────────────────────
data/
output/
logs/

# Markierung, dass die Pakete auf DIESEM Rechner eingerichtet sind. Käme sie mit ins
# Repository, würde der nächste Rechner die Erstinstallation überspringen.
.eingerichtet

# Interne Arbeitsnotizen — sie nennen Pfade und Kontenverhältnisse, die in einem
# öffentlichen Repository nichts verloren haben.
CLAUDE.md
*.db
*.db-shm
*.db-wal

# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.coverage

# ── Betriebssystem / Editor ─────────────────────────────────────────────────
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# Interne KI-Notizen (aus CLAUDE.md ausgelagert, 18.09.2026) - wie CLAUDE.md nicht veroeffentlichen
docs/VERLAUF.md
docs/archiv/
